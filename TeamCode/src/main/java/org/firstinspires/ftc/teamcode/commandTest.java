package org.firstinspires.ftc.teamcode;

import static com.pedropathing.ivy.groups.Groups.sequential;

import com.pedropathing.ivy.Command;
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.pedropathing.ivy.Scheduler;
import com.qualcomm.robotcore.hardware.DcMotor;


public class commandTest {

    /* think for us commands most useful for subsystems like intakes and arms. auto aligning turret or placement system???

    Command myFirstCommand = Command.build()
            .setStart(() -> {
                // executed on start; this could be setting parameters like set target on PID loop
            })
            .setExecute(() -> {
                // executed on execute; whats going to happen
            })
            .setDone(() -> {
                // return true to end the command; when it will stop
            })
            .setEnd(endCondition -> {
                // executed on end; likely set power to 0 or return to position
            })
            .requiring(/* requirements )
            .setInterruptedBehavior( interrupted behavior )
            .setBlockedBehavior( blocked behavior )
            .setConflictBehavior( conflict behavior )
            .setPriority( priority );
     */
}
